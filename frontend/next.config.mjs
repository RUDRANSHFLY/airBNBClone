/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'links.papareact.com',
            'upload.wikimedia.org',
            'media.istockphoto.com'
        ]
    },
};

export default nextConfig;
